# Learning: S4 verdict — UserPromptSubmit block-as-answer WORKS with zero model invocation; 

## Summary
S4 verdict — UserPromptSubmit block-as-answer WORKS with zero model invocation; WINNER: decision:block JSON (over exit-2 stderr). Empirically observed in CC 2.1.205 on 2026-07-10 (TASK-57 spike). Both variants blocked the trigger turn with num_turns=0, duration_api_ms=0, all-zero usage and modelUsage={} — a genuinely zero-token deterministic answer path for Tier-1. Rendering: decision:block shows 'UserPromptSubmit operation blocked by hook:' plus the reason (clean); exit-2 leaks the raw hook command chrome into the visible answer. Echo hygiene (mem-034): the same-session follow-up prompt passed through without re-trigger and answered normally with no block-text leakage; caution — Claude Code itself appends 'Original prompt: <trigger>' to the block message, so the trigger phrase re-enters the transcript regardless of hook-side hygiene; transcript/context-scanning hooks must tolerate that echo. Scripting note: a blocked turn still returns subtype success, exit 0 — detect blocks via num_turns==0 or the result prefix, never exit codes. Evidence: transcripts /Users/aleks.petrov/.claude/projects/-private-tmp-nav-v7-spike-project/69c208c3-55b4-4c9b-b607-142af26fddfe.jsonl (decision:block) and 58150d0e-5aa9-4afd-a9b1-76d86accbcc9.jsonl (exit-2); probe /tmp/nav-v7-spike/probes/probe_s4.py; verdict /tmp/nav-v7-spike/state/verdict-s4.json.

## Context
Discovered during development.

## Details
S4 verdict — UserPromptSubmit block-as-answer WORKS with zero model invocation; WINNER: decision:block JSON (over exit-2 stderr). Empirically observed in CC 2.1.205 on 2026-07-10 (TASK-57 spike). Both variants blocked the trigger turn with num_turns=0, duration_api_ms=0, all-zero usage and modelUsage={} — a genuinely zero-token deterministic answer path for Tier-1. Rendering: decision:block shows 'UserPromptSubmit operation blocked by hook:' plus the reason (clean); exit-2 leaks the raw hook command chrome into the visible answer. Echo hygiene (mem-034): the same-session follow-up prompt passed through without re-trigger and answered normally with no block-text leakage; caution — Claude Code itself appends 'Original prompt: <trigger>' to the block message, so the trigger phrase re-enters the transcript regardless of hook-side hygiene; transcript/context-scanning hooks must tolerate that echo. Scripting note: a blocked turn still returns subtype success, exit 0 — detect blocks via num_turns==0 or the result prefix, never exit codes. Evidence: transcripts /Users/aleks.petrov/.claude/projects/-private-tmp-nav-v7-spike-project/69c208c3-55b4-4c9b-b607-142af26fddfe.jsonl (decision:block) and 58150d0e-5aa9-4afd-a9b1-76d86accbcc9.jsonl (exit-2); probe /tmp/nav-v7-spike/probes/probe_s4.py; verdict /tmp/nav-v7-spike/state/verdict-s4.json.

## Recommended Approach
Apply this knowledge when working on related topics.

## Related
- None documented

---
**Captured**: 2026-07-10
**Confidence**: 100%
**Concepts**: hooks, harness-behavior, plugin-channels
