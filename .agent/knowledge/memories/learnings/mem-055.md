# Learning: S6 verdict — ${CLAUDE_PLUGIN_ROOT} binds correctly in plugin-manifest hook comma

## Summary
S6 verdict — ${CLAUDE_PLUGIN_ROOT} binds correctly in plugin-manifest hook commands, including headless claude -p sessions. Empirically observed in CC 2.1.205 on 2026-07-10 (TASK-57 spike). For directory-source marketplaces it binds to the marketplace SOURCE path (/tmp/nav-v7-spike/marketplace/nav-spike), not the ~/.claude/plugins/cache copy — source edits take effect in new sessions without reinstall. Unset/empty env is a LOUD failure (exit 2, python cannot open /hooks/...), not mem-036's silent no-op — the :-fallback pattern stays prudent for commands that might run outside plugin context. SessionStart DOES fire in headless -p sessions. Two conformance-relevant harness facts: hooks that print no stdout leave NO transcript attachment (silent hooks are transcript-invisible; suites need side-channel logs), and hook payload cwd arrives realpath'd on macOS (/tmp becomes /private/tmp) while $PWD may stay logical — never gate hooks on raw string-prefix cwd checks; resolve paths first. Evidence: side-channel log /tmp/nav-v7-spike/state/log-s6.txt; transcripts /Users/aleks.petrov/.claude/projects/-private-tmp-nav-v7-spike-project/4c6781cf-54a1-47db-a47b-76e14b700ef6.jsonl and 4d1391c6-1862-44cf-ba46-baca113d5c85.jsonl; probe /tmp/nav-v7-spike/probes/probe_s6.py; verdict /tmp/nav-v7-spike/state/verdict-s6.json.

## Context
Discovered during development.

## Details
S6 verdict — ${CLAUDE_PLUGIN_ROOT} binds correctly in plugin-manifest hook commands, including headless claude -p sessions. Empirically observed in CC 2.1.205 on 2026-07-10 (TASK-57 spike). For directory-source marketplaces it binds to the marketplace SOURCE path (/tmp/nav-v7-spike/marketplace/nav-spike), not the ~/.claude/plugins/cache copy — source edits take effect in new sessions without reinstall. Unset/empty env is a LOUD failure (exit 2, python cannot open /hooks/...), not mem-036's silent no-op — the :-fallback pattern stays prudent for commands that might run outside plugin context. SessionStart DOES fire in headless -p sessions. Two conformance-relevant harness facts: hooks that print no stdout leave NO transcript attachment (silent hooks are transcript-invisible; suites need side-channel logs), and hook payload cwd arrives realpath'd on macOS (/tmp becomes /private/tmp) while $PWD may stay logical — never gate hooks on raw string-prefix cwd checks; resolve paths first. Evidence: side-channel log /tmp/nav-v7-spike/state/log-s6.txt; transcripts /Users/aleks.petrov/.claude/projects/-private-tmp-nav-v7-spike-project/4c6781cf-54a1-47db-a47b-76e14b700ef6.jsonl and 4d1391c6-1862-44cf-ba46-baca113d5c85.jsonl; probe /tmp/nav-v7-spike/probes/probe_s6.py; verdict /tmp/nav-v7-spike/state/verdict-s6.json.

## Recommended Approach
Apply this knowledge when working on related topics.

## Related
- None documented

---
**Captured**: 2026-07-10
**Confidence**: 100%
**Concepts**: hooks, harness-behavior, plugin-channels, deployment
