# Learning: Claude Code 2.1.258: the LSP tool is grantable to a subagent by listing LSP in t

## Summary
Claude Code 2.1.258: the LSP tool is grantable to a subagent by listing LSP in tools: (agent .md frontmatter or --agents JSON); listing it without a language-server plugin is harmless (agent loads, zero LSP calls, no error). pyright's first request after server start races workspace indexing — findReferences returns only the definition, a second identical call returns the full set (61 refs for runtime.dispatch). Evidence: TASK-76 spike transcripts 7cd03832…/agent-af6a7fa4a596a9b78.jsonl (grant), 27906176…/agent-ae808b30ec9fbda5a.jsonl (negative control), e9008f11…/subagents (retry → 61).

## Context
Discovered during development.

## Details
Claude Code 2.1.258: the LSP tool is grantable to a subagent by listing LSP in tools: (agent .md frontmatter or --agents JSON); listing it without a language-server plugin is harmless (agent loads, zero LSP calls, no error). pyright's first request after server start races workspace indexing — findReferences returns only the definition, a second identical call returns the full set (61 refs for runtime.dispatch). Evidence: TASK-76 spike transcripts 7cd03832…/agent-af6a7fa4a596a9b78.jsonl (grant), 27906176…/agent-ae808b30ec9fbda5a.jsonl (negative control), e9008f11…/subagents (retry → 61).

## Recommended Approach
Apply this knowledge when working on related topics.

## Related
- None documented

---
**Captured**: 2026-09-13
**Confidence**: 95%
**Concepts**: hooks, subagents, harness-behavior, lsp
