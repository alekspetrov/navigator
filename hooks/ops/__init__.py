"""hooks/ops — Navigator v7 op modules (TASK-61/62), one file per op.

Empty package marker. See README.md in this directory for the op protocol
(run(ctx) -> result dict) and phase semantics; see nav_hook_lib/registry.py
for the EVENT -> OpSpec routing that names the modules expected here.
"""
